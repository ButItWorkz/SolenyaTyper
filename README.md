# SolenyaTyper
A lightweight, native utility designed to bypass client-side paste restrictions (such as JavaScript onpaste blocks) via asynchronous hardware interrupt polling and raw keystroke simulation.
